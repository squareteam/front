'use strict';

describe('Directive: st-user-profile', function () {

  // // load the directive's module
  // beforeEach(module('squareteam.app'));

  // var element,
  //   scope;

  // beforeEach(inject(function ($rootScope) {
  //   scope = $rootScope.$new();
  // }));

  // it('should make hidden element visible', inject(function ($compile) {
  //   element = angular.element('<stuserprofile></stuserprofile>');
  //   element = $compile(element)(scope);
  //   expect(element.text()).toBe('this is the stuserprofile directive');
  // }));
});
